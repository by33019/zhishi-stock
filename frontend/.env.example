# 前端环境变量样例
#
# 用法：
#   cp .env.example .env.local     # .env.local 已被 .gitignore 忽略，不会入库
#
# ⚠️ 安全提醒：Vite 只会把 VITE_ 前缀的变量注入客户端代码，
#    因此这里**不要**填写任何密钥、令牌或服务端凭据——它们会出现在打包产物中。

# 后端接口前缀。读取位置：src/services/apiClient.ts
#
# 默认值 /api/v1 适用于**容器部署**：nginx 已配置反向代理 /api/ → stock-api:8080，
# 浏览器只需访问 http://localhost:8088 即可，前后端同源、无跨域问题。
#
# ⚠️ 已知限制（见 TASKS.md M1-16）：
#   本地 `npm run dev` 目前**无法**取到真实接口数据。原因是开发服务器未配置代理，
#   后端也未开启 CORS，请求 /api/v1/** 会被 Vite 的 SPA fallback 返回 index.html。
#   本地联调请改用容器路径：docker-compose up -d --build 后访问 http://localhost:8088
VITE_API_BASE_URL=/api/v1
