# Agent 工作约定

本文件用于说明在本项目中协作的 Agent 工作方式和项目约定。

## 项目概况

## 沟通语言

- 后续关于本项目的所有沟通统一使用中文。
- 代码、命令、文件名和技术专有名词可根据实际情况保留英文。
- 如必须引用英文报错或原始文档内容，应同时提供中文说明。

## 工作规则

- 优先进行小范围、目标明确的修改。
- 保留用户已有的工作内容，避免覆盖无关改动。
- 在做出技术栈或运行环境假设前，先检查项目结构和相关文件。
- 新增或修改文件时，保持与项目现有格式一致。
- 如果需求不完整或变更依赖缺失信息，应在继续前明确说明所采用的假设。

## GitHub 版本管理

- 本项目使用 GitHub 仓库 `git@github.com:by33019/zhishi-stock.git` 管理版本。
- 默认开发分支为 `main`，未经明确要求不得强制推送或改写远程历史。
- 每次修改后的成果说明、提交信息和 Pull Request 描述统一使用中文。
- 提交信息应简洁说明本次变更目的，推荐使用 `类型：中文描述`，例如 `功能：完成市场总览前端界面`。
- 提交前检查待提交文件，禁止提交密钥、访问令牌、真实环境配置、依赖目录和构建产物。

## 推荐工作流程

1. 先检查相关文件和项目结构。
2. 实施满足需求所需的最小改动。
3. 条件允许时执行相应的验证或测试。
4. 用中文总结修改内容、验证结果和仍需注意的问题。

## 行为准则（Karpathy 原则）

1. **先思后写**：澄清需求，列出假设。在产出代码前，先确认问题边界、技术方案，避免盲目编码。
2. **简洁至上**：避免过度抽象。用最少的代码实现功能，不提前引入设计模式或未来可能需要的灵活性。
3. **精准修改**：只改任务相关代码。不顺手“优化”无关模块，不进行与当前任务无关的重构。
4. **测试驱动**：先写失败测试，再实现功能。确保每个新功能都有对应的测试用例，且修改后所有测试通过。

## 备注

- 随着代码库的发展，在此补充项目专属规则。
- 如果项目后续增加构建、测试或部署流程，应在此记录相关说明。
